// Organization context store
// Will be implemented with zustand or similar state management

export interface Organization {
  id: string;
  name: string;
  slug: string;
}

// Placeholder for organization store
// Implementation will depend on chosen state management library
